import AppShell from "./AppShell";

export default function ConnectionsPage() {
  const rows = [["Binance Futures", "公开行情适配器", "CONNECTED", "24h ticker · OI · funding · K-lines"], ["Paper worker", "本地 SQLite 模拟账本", "RUNNING", "自动扫描 · 开仓 · 盯仓 · 平仓"], ["Local MCP Agent", "本地 MCP Client ↔ Server", "CONNECTED", "8 个只读工具 · 中文对话 · Tool Trace"], ["Official Binance MCP", "官方 Agent OS 远程服务", "AUTH REQUIRED", "https://agent.binance.com/mcp/agentic · 需在官方客户端授权"], ["Agent Tool Layer", "内部五阶段编排", "READY", "策略判断 · 风险闸门 · Paper 计划 · 审计记录"], ["Real execution", "真实账户交易接口", "DISABLED", "当前产品边界：不读取密钥、不签名、不广播"]];
  return <AppShell title="权限与连接" eyebrow="SYSTEM BOUNDARIES"><div className="connections-hero"><div><span className="page-eyebrow">EXPLICIT PERMISSION MODEL</span><h2>看得见数据，<em>守得住边界。</em></h2><p>这个参赛原型使用 Binance Futures 公开行情和本地 Paper worker；官方 Binance MCP 仅展示连接入口，授权动作仍由你在官方客户端完成。</p></div><div className="permission-lock">NO<br /><strong>WITHDRAWAL</strong></div></div><div className="connection-list">{rows.map(([name, type, status, detail]) => <div className="connection-row" key={name}><span className={`connection-icon ${status === "DISABLED" ? "off" : status === "AUTH REQUIRED" ? "watch" : ""}`}>{status === "DISABLED" ? "×" : status === "AUTH REQUIRED" ? "!" : "✓"}</span><div><strong>{name}</strong><small>{type}</small></div><span className={`status-chip ${status === "DISABLED" ? "off" : status === "AUTH REQUIRED" ? "watch" : "live"}`}>{status}</span><p>{detail}</p></div>)}</div><div className="safety-note">当前安全声明：Paper execution only · broadcast=false · no withdrawal · no signing</div></AppShell>;
}
