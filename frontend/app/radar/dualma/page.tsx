import AppShell from "../../components/AppShell";
import StrategyWorkspace from "../../components/StrategyWorkspace";

export default function DualMaPage() {
  return <AppShell title="双均线 4H" eyebrow="STRATEGY WORKSPACE / 02"><StrategyWorkspace mode="dualma" /></AppShell>;
}
